server:
  port: 8812
spring:
  application:
    name: eureka-clientC
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config-server
      name: eureka-clientC
      username: yao
      password: 123
  zipkin:
    base-url: http://localhost:9411

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8800/eureka/



